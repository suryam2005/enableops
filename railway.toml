[build]
builder = "nixpacks"

[deploy]
startCommand = "python enablebot/scripts/start_web.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production]
variables = { }